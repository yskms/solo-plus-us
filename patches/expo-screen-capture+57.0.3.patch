diff --git a/node_modules/expo-screen-capture/android/src/main/java/expo/modules/screencapture/ScreenCaptureModule.kt b/node_modules/expo-screen-capture/android/src/main/java/expo/modules/screencapture/ScreenCaptureModule.kt
index e76da2f..2e1b127 100644
--- a/node_modules/expo-screen-capture/android/src/main/java/expo/modules/screencapture/ScreenCaptureModule.kt
+++ b/node_modules/expo-screen-capture/android/src/main/java/expo/modules/screencapture/ScreenCaptureModule.kt
@@ -92,6 +92,25 @@ class ScreenCaptureModule : Module() {
       currentActivity.window.clearFlags(WindowManager.LayoutParams.FLAG_SECURE)
     }.runOnQueue(Queues.MAIN)
 
+    // solo-plus-us-app patch (new function, not upstream): Android 13+
+    // only. `Activity.setRecentsScreenshotEnabled(false)` hides the
+    // Recent Apps thumbnail WITHOUT blocking screenshots/recording
+    // elsewhere in the app — unlike FLAG_SECURE (preventScreenCapture
+    // above), which couples the two on every Android version. This is
+    // what lets the app default to "Recent Apps preview hidden,
+    // screenshots allowed" instead of blocking both. No-op below API 33,
+    // where no such split API exists — callers must fall back to
+    // preventScreenCapture there (see lib/screenMask.ts).
+    AsyncFunction("setRecentsScreenshotEnabled") { enabled: Boolean ->
+      if (Build.VERSION.SDK_INT >= Build.VERSION_CODES.TIRAMISU) {
+        try {
+          currentActivity.setRecentsScreenshotEnabled(enabled)
+        } catch (error: Exception) {
+          Log.e("ExpoScreenCapture", "Could not set recents screenshot enabled: ${error.message}")
+        }
+      }
+    }.runOnQueue(Queues.MAIN)
+
     OnActivityEntersForeground {
       // Call registerCallback once more as a fallback if activity wasn't available in onCreate
       registerCallback()
@@ -104,9 +123,16 @@ class ScreenCaptureModule : Module() {
 
     OnDestroy {
       screenshotEventEmitter?.onHostDestroy()
-      if (Build.VERSION.SDK_INT >= Build.VERSION_CODES.UPSIDE_DOWN_CAKE) {
-        screenCaptureCallback?.let {
-          safeCurrentActivity?.unregisterScreenCaptureCallback(it)
+      // solo-plus-us-app patch: only unregister if registerCallback() above
+      // actually succeeded — same DETECT_SCREEN_CAPTURE permission gap as
+      // registerCallback(), guarded the same way (see that comment).
+      if (Build.VERSION.SDK_INT >= Build.VERSION_CODES.UPSIDE_DOWN_CAKE && isRegistered) {
+        try {
+          screenCaptureCallback?.let {
+            safeCurrentActivity?.unregisterScreenCaptureCallback(it)
+          }
+        } catch (error: Exception) {
+          Log.e("ExpoScreenCapture", "Could not unregister screen capture callback: ${error.message}")
         }
       }
     }
@@ -117,8 +143,21 @@ class ScreenCaptureModule : Module() {
       return
     }
     if (Build.VERSION.SDK_INT >= Build.VERSION_CODES.UPSIDE_DOWN_CAKE) {
-      safeCurrentActivity?.registerScreenCaptureCallback(currentActivity.mainExecutor, screenCaptureCallback!!) ?: return
-      isRegistered = true
+      try {
+        safeCurrentActivity?.registerScreenCaptureCallback(currentActivity.mainExecutor, screenCaptureCallback!!) ?: return
+        isRegistered = true
+      } catch (error: Exception) {
+        // solo-plus-us-app patch: apps that intentionally don't declare
+        // android.permission.DETECT_SCREEN_CAPTURE (this app strips it via
+        // plugins/withoutScreenCaptureDetectionPermissions.js, since it
+        // never uses the screenshot "detection" feature — see that file's
+        // doc comment) crash here on API 34+, because this OnCreate path
+        // calls registerScreenCaptureCallback() unconditionally regardless
+        // of whether the permission is held. Swallow it the same way the
+        // pre-34 ScreenshotEventEmitter path already treats a missing
+        // permission: log and continue, never crash.
+        Log.e("ExpoScreenCapture", "Could not register screen capture callback, missing DETECT_SCREEN_CAPTURE permission: ${error.message}")
+      }
     }
   }
 }
diff --git a/node_modules/expo-screen-capture/build/ScreenCapture.d.ts b/node_modules/expo-screen-capture/build/ScreenCapture.d.ts
index 1be8349..d6acbf1 100644
--- a/node_modules/expo-screen-capture/build/ScreenCapture.d.ts
+++ b/node_modules/expo-screen-capture/build/ScreenCapture.d.ts
@@ -66,6 +66,14 @@ export declare function enableAppSwitcherProtectionAsync(blurIntensity?: number)
  * @platform ios
  */
 export declare function disableAppSwitcherProtectionAsync(): Promise<void>;
+/**
+ * solo-plus-us-app patch (new function, not upstream): hides the Recent
+ * Apps thumbnail without blocking screenshots/recording elsewhere in the
+ * app. No-op below Android 13 or on iOS/web.
+ *
+ * @platform android
+ */
+export declare function setRecentsScreenshotEnabledAsync(enabled: boolean): Promise<void>;
 /**
  * Adds a listener that will fire whenever the user takes a screenshot while the app is foregrounded.
  *
diff --git a/node_modules/expo-screen-capture/build/ScreenCapture.js b/node_modules/expo-screen-capture/build/ScreenCapture.js
index 8b6d485..858504d 100644
--- a/node_modules/expo-screen-capture/build/ScreenCapture.js
+++ b/node_modules/expo-screen-capture/build/ScreenCapture.js
@@ -111,6 +111,17 @@ export async function disableAppSwitcherProtectionAsync() {
     }
     await ExpoScreenCapture.disableAppSwitcherProtection();
 }
+// solo-plus-us-app patch (new function, not upstream): see the Kotlin
+// side's doc comment (ScreenCaptureModule.kt) for why this exists
+// separately from preventScreenCaptureAsync. No-op below Android 13 or
+// on iOS/web, where the native function doesn't exist — mirrors this
+// file's own style for other optional native functions.
+export async function setRecentsScreenshotEnabledAsync(enabled) {
+    if (!ExpoScreenCapture.setRecentsScreenshotEnabled) {
+        return;
+    }
+    await ExpoScreenCapture.setRecentsScreenshotEnabled(enabled);
+}
 // @needsAudit
 /**
  * Adds a listener that will fire whenever the user takes a screenshot while the app is foregrounded.
diff --git a/node_modules/expo-screen-capture/expo-module.config.json b/node_modules/expo-screen-capture/expo-module.config.json
index 31b4467..9ab3900 100644
--- a/node_modules/expo-screen-capture/expo-module.config.json
+++ b/node_modules/expo-screen-capture/expo-module.config.json
@@ -4,12 +4,6 @@
     "modules": ["ScreenCaptureModule"]
   },
   "android": {
-    "modules": ["expo.modules.screencapture.ScreenCaptureModule"],
-    "publication": {
-      "groupId": "host.exp.exponent",
-      "artifactId": "expo.modules.screencapture",
-      "version": "57.0.3",
-      "repository": "local-maven-repo"
-    }
+    "modules": ["expo.modules.screencapture.ScreenCaptureModule"]
   }
 }
diff --git a/node_modules/expo-screen-capture/src/ScreenCapture.ts b/node_modules/expo-screen-capture/src/ScreenCapture.ts
index d11d71b..b067730 100644
--- a/node_modules/expo-screen-capture/src/ScreenCapture.ts
+++ b/node_modules/expo-screen-capture/src/ScreenCapture.ts
@@ -126,6 +126,22 @@ export async function disableAppSwitcherProtectionAsync(): Promise<void> {
   await ExpoScreenCapture.disableAppSwitcherProtection();
 }
 
+/**
+ * solo-plus-us-app patch (new function, not upstream): hides the Recent
+ * Apps thumbnail without blocking screenshots/recording elsewhere in the
+ * app. No-op below Android 13 or on iOS/web — see the Kotlin side
+ * (ScreenCaptureModule.kt) for why this exists separately from
+ * `preventScreenCaptureAsync`.
+ *
+ * @platform android
+ */
+export async function setRecentsScreenshotEnabledAsync(enabled: boolean): Promise<void> {
+  if (!ExpoScreenCapture.setRecentsScreenshotEnabled) {
+    return;
+  }
+  await ExpoScreenCapture.setRecentsScreenshotEnabled(enabled);
+}
+
 // @needsAudit
 /**
  * Adds a listener that will fire whenever the user takes a screenshot while the app is foregrounded.
